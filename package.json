{
  "name": "@adapt-retail/banner-data",
  "version": "0.0.0",
  "description": "Api for retreaving Adapt Retail information to banners.",
  "main": "dist/index.js",
  "scripts": {
    "test": "ava",
    "tdd": "executor 'npm run build && npm run test' --watch='src/*.js' --watch='test/*.js'",
    "build": "babel src -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adaptretail/banner-data.git"
  },
  "bugs": "https://github.com/adaptretail/banner-data/issues",
  "keywords": [],
  "author": "Lasse S. Haslev",
  "license": "ISC",
  "devDependencies": {
    "@lassehaslev/executor": "^0.1.2",
    "ava": "^0.21.0",
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "extend": "^3.0.1"
  }
}
