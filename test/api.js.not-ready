import test from 'ava';
import AdaptData from '../dist/index';

global.window = {};

// test.before( t => {
    // global.window = null;
// } )

test( 'is setting full path when adding account options', t => {

    let data = new AdaptData( {
        account: 'account',
        project: 'project',
        campaign: 'campaign',
        production: 'production',
        source: null,
    } )

    console.log(data.folder());

    t.is( 
        data.asset( 'folder' ),
        'https://cdn.adaptretail.com/account/project/project/campaign/campaign/production/production/live/folder'
    );

} );

// can get folder path
// 
